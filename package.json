{
  "name": "eu-ai-act-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An independent, educational guide to the EU AI Act for non-technical people.",
  "license": "MIT",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate && astro build && node scripts/validate-generated.mjs",
    "preview": "astro preview",
    "test": "node --test tests/**/*.test.mjs",
    "test:decision": "node --test tests/decision/*.test.mjs",
    "translations:stamp": "node scripts/stamp-translations.mjs",
    "validate": "npm run validate:locales && npm run validate:sources && npm run validate:skill",
    "validate:locales": "node scripts/validate-locales.mjs",
    "validate:sources": "node scripts/source/verify-archive.mjs",
    "validate:skill": "node scripts/validate-skill-package.mjs",
    "package:skill": "node scripts/package-skill.mjs"
  },
  "devDependencies": {
    "astro": "7.1.6"
  }
}
